## ADDED Requirements

### Requirement: Profit/loss report

The dashboard SHALL include a Profit report page at `/dashboard/reports/profits`.

#### Scenario: Profit report renders

- **WHEN** a user with `profits-access` visits the profit report page
- **THEN** they SHALL see a profit/loss summary with date range filter
