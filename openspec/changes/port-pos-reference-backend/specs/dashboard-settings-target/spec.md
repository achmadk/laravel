## ADDED Requirements

### Requirement: Sales target settings

The dashboard SHALL include a Sales Target settings page at `/dashboard/settings/target`.

#### Scenario: Target settings renders

- **WHEN** a user visits `/dashboard/settings/target`
- **THEN** they SHALL see current sales target and progress
