## ADDED Requirements

### Requirement: CRM Reminder list

The dashboard SHALL include a CRM Reminders index page at `/dashboard/crm-reminders`.

#### Scenario: Reminder index renders

- **WHEN** a user with `crm-reminders-access` visits `/dashboard/crm-reminders`
- **THEN** they SHALL see a list of generated reminders with status
